from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='SVM in order to predict if someone will default on their credit card payments',
    author='Theo Eudes',
    license='MIT',
)
